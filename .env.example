# Example .env file for the React frontend application.

# Base URL for the backend API.
# During development, Vite can proxy requests from /api to this URL.
# In production, ensure your hosting environment (e.g., Vercel rewrites)
# is configured to point /api to your deployed Flask backend.
VITE_API_BASE_URL=http://localhost:5000/api
